# Dev-C++ 使用指南

## 什么是 Dev-C++？

Dev-C++ 是一个免费的 Windows C/C++ 集成开发环境（IDE），基于 MinGW 编译器。

## 下载和安装 Dev-C++

### 推荐版本：Dev-C++ 6.3（或更高）

1. 下载地址：
   - 官方网站：https://sourceforge.net/projects/orwelldevcpp/
   - 备用（Embarcadero版本）：https://www.embarcadero.com/free-tools/dev-cpp

2. 安装步骤：
   - 双击安装程序
   - 选择简体中文
   - 选择"完全安装"
   - 确保勾选"TDM-GCC"编译器

## 在 Dev-C++ 中打开项目

### 方法1：使用项目文件（推荐）

1. 启动 Dev-C++
2. 点击菜单：文件 → 打开项目
3. 选择 `QingEmulator.dev` 文件
4. 项目会自动加载所有源文件

### 方法2：手动创建项目

如果 `.dev` 文件无法打开，可以手动创建：

1. 启动 Dev-C++
2. 文件 → 新建 → 项目
3. 选择"Console Application"（控制台应用程序）
4. 选择"C++ Project"
5. 命名为"QingEmulator"
6. 添加所有源文件：
   - main.cpp
   - Game.cpp
   - Game.h
   - Province.cpp
   - Province.h
   - General.cpp
   - General.h
   - Technology.h
   - Event.h

## 配置编译选项

### 设置 C++11 标准

1. 点击菜单：工具 → 编译选项
2. 在"代码生成/优化"标签页
3. 找到"语言标准(-std)"
4. 选择"ISO C++11"或"GNU C++11"
5. 点击"确定"

### 或者手动添加编译参数

1. 工具 → 编译选项
2. 在"编译器"标签页的"在编译器命令行加入以下命令"中添加：
   ```
   -std=c++11
   ```

## 编译和运行

### 快速编译运行（推荐）

1. 按 `F11` 键（或点击菜单：运行 → 编译运行）
2. Dev-C++ 会自动编译并运行程序

### 分步操作

1. **仅编译**：按 `F9` 或 点击菜单：运行 → 编译
2. **仅运行**：按 `F10` 或 点击菜单：运行 → 运行
3. **重新编译所有**：按 `Ctrl+F11` 或 点击菜单：运行 → 重新编译所有

## 解决中文乱码问题

### 方法1：设置编码（推荐）

1. 点击菜单：工具 → 编辑器选项
2. 在"常规"标签页
3. 找到"编辑器字体"
4. 选择支持中文的字体，如"新宋体"或"微软雅黑"
5. 在"默认编码"中选择"UTF-8"或"GB2312"

### 方法2：修改源代码

如果运行时中文显示乱码，修改 `main.cpp`：

```cpp
#ifdef _WIN32
    // 使用 GBK 编码而不是 UTF-8
    SetConsoleOutputCP(936);
    SetConsoleCP(936);
#endif
```

### 方法3：保存文件为 GBK 编码

1. 在 Dev-C++ 中打开源文件
2. 文件 → 另存为
3. 在编码选项中选择"ANSI"或"GB2312"
4. 重新编译

## 常见问题

### Q1: 编译错误："'make_unique' is not a member of 'std'"

**A**: 这是因为使用了 C++14 特性。代码已经修改为 C++11 兼容，确保：
- 使用最新版本的代码
- 编译选项设置为 C++11

### Q2: 编译错误："undefined reference to 'WinMain@16'"

**A**: 项目类型设置错误，应该是"Console Application"而不是"Windows Application"。
- 重新创建项目，选择正确的项目类型

### Q3: 运行时窗口一闪而过

**A**: 在 `main.cpp` 的 `return 0;` 之前添加：
```cpp
system("pause");
```

或者在 Dev-C++ 中运行时会自动暂停。

### Q4: 编译时出现很多警告

**A**: 可以忽略警告，只要没有错误即可。如果想关闭警告：
- 工具 → 编译选项 → 编译器
- 取消勾选"显示警告"

### Q5: 中文显示为乱码或方块

**A**: 按照"解决中文乱码问题"部分的方法解决。

### Q6: 编译速度很慢

**A**: 正常现象，特别是第一次编译。后续编译会更快。

## 调试程序

### 设置断点

1. 在代码行号处点击，会出现红点（断点）
2. 按 `F8` 开始调试
3. 使用 `F7`（单步执行）或 `F6`（下一步）调试

### 查看变量

调试时可以：
- 鼠标悬停在变量上查看值
- 使用"调试"窗口查看变量

## 优化建议

### 编译优化

1. 工具 → 编译选项
2. 在"代码生成/优化"标签页
3. "优化级别"选择"最高(-O3)"

### 生成独立可执行文件

如果要分享给他人，需要包含 MinGW 的 DLL 文件，或者：

1. 工具 → 编译选项 → 编译器
2. 在"链接器"中添加：
   ```
   -static-libgcc -static-libstdc++
   ```

这样生成的 exe 文件可以独立运行。

## 项目文件说明

```
QingEmulator/
├── main.cpp           # 主程序入口
├── Game.h             # 游戏类头文件
├── Game.cpp           # 游戏类实现
├── Province.h         # 省份类头文件
├── Province.cpp       # 省份类实现
├── General.h          # 将领类头文件
├── General.cpp        # 将领类实现
├── Technology.h       # 科技结构体
├── Event.h            # 事件结构体
└── QingEmulator.dev   # Dev-C++ 项目文件
```

## 导出为可执行文件

编译成功后，可执行文件位置：
- 默认在项目文件夹中：`QingEmulator.exe`

可以将此文件复制到其他地方运行。

## 快捷键参考

| 功能 | 快捷键 |
|------|--------|
| 编译 | F9 |
| 运行 | F10 |
| 编译并运行 | F11 |
| 重新编译所有 | Ctrl+F11 |
| 调试 | F8 |
| 单步执行 | F7 |
| 保存 | Ctrl+S |
| 查找 | Ctrl+F |
| 替换 | Ctrl+H |

## 推荐设置

### 编辑器设置

1. 工具 → 编辑器选项
2. 显示行号：勾选
3. 自动缩进：勾选
4. Tab 宽度：4 空格
5. 字体大小：根据屏幕选择（推荐 10-12）

### 代码提示

Dev-C++ 自带代码自动完成功能，输入时会自动提示。

## 获取帮助

如果遇到问题：
1. 查看编译输出窗口的错误信息
2. 复制错误信息搜索解决方案
3. 确保使用的是最新版本的 Dev-C++
4. 确保源文件编码正确

祝您使用愉快！
